services:
    trendsense-api:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: trendsense-api
        ports:
            - "8080:8080"
        environment:
            ASPNETCORE_ENVIRONMENT: Development
            ASPNETCORE_URLS: https://+:8080
            Jwt__Key: ${JWT_KEY}